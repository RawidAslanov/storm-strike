# Локальная разработка — скопируй в .env (не коммить!)
# VITE_WS_URL=ws://localhost:3001

# ── Продакшен ──────────────────────────────────────────────
# Vercel (клиент): Settings → Environment Variables
#   VITE_WS_URL = wss://ВАШ-СЕРВЕР.onrender.com
#
# Render (сервер): PORT задаётся автоматически
